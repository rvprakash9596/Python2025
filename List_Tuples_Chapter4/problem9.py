# Store the below values in a list & sort them from “A” to “D”
# grade = ("C","D","A","E","A","A")

grade = ["C","D","A","E","A","A"]
grade.sort(reverse=True) # printing reverse order
print(grade)

grade.sort()
print(grade) # printing ascebding order